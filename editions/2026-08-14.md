# AI/LLM Briefing — 14 August 2026

## Telegram — Run 07:00

DeepSeek published the open weights of its flagship, DeepSeek-V4-Pro-0813, on Hugging Face under an MIT licence — anyone can now download and run it.

It's a mixture-of-experts model — only a slice runs per query: 6 of its 384 expert sub-networks fire on each token. DeepSeek calls it the official release, replacing an earlier preview.

"DeepSeek-V4-Pro-0813 is the official release of DeepSeek-V4-Pro, superseding the preview version."

the open-weights frontier keeps closing on the closed one.

#DeepSeek #OpenWeights #LLM

https://huggingface.co/deepseek-ai/DeepSeek-V4-Pro-0813

## Notes (archive only — not sent to Telegram)

### Run 07:00 — chosen beat / sources / thread updates

First slot of the day (Friday). Ran the verified-feed spine + active-thread watch-fors + net-new (~9 lookups across the five categories) since the 13 Aug 20:00 run (which OPENED + POSTED T17 on OpenAI's Ultrafast/Cerebras inference tier).

- **CHOSEN (ADVANCE T15 — DeepSeek open-weights frontier line):** the **open weights of `deepseek-ai/DeepSeek-V4-Pro-0813` are now public on Hugging Face** — gated:No, MIT licence, 66 `.safetensors` weight shards (~1.65 TB, FP8), 336 likes. This is the exact T15 Watch-for: "the 0813 open weights actually landing on `huggingface.co/deepseek-ai` (would let an open-weights beat ship — currently API-only)". The 13 Aug beats shipped the API/pricing GA (07:00) and the AA Index 53 (13:00) and both explicitly recorded the weights as **not on HF / API-only**; the 13 Aug 20:00 run re-checked and logged "`huggingface.co/deepseek-ai` newest still 31 Jul `DeepSeek-V4-Flash-0731` … the 0813 Pro GA remains API-only" and flagged `TechnoBaptist/DeepSeek-V4-Pro-0813` as a **community** re-upload (not `deepseek-ai`). The official `deepseek-ai` repo is now live and downloadable — a distinct, materially-new fact (open-weights availability of a frontier model), not a repeat of the API/pricing or AA-benchmark beats → legitimate ADVANCE. The HF model card + `config.json` are **allowlisted primaries that render to the fetcher** — no scoped-search workaround needed.
  - **Source-anchored claims:** (all anchored to `huggingface.co/deepseek-ai/DeepSeek-V4-Pro-0813` — card, `config.json`, and the file listing, all opened this session)
    - "DeepSeek published the open weights of its flagship, DeepSeek-V4-Pro-0813, on Hugging Face … anyone can now download and run it" <- the `deepseek-ai/DeepSeek-V4-Pro-0813` repo exists, `gated: false`, with 66 `model-*-of-00066.safetensors` weight shards present and downloadable
    - "under an MIT licence" <- card: "This repository and the model weights are licensed under the MIT License."
    - "a mixture-of-experts model … 6 of its 384 expert sub-networks active on each token" <- `config.json`: `"model_type": "deepseek_v4"`, `"n_routed_experts": 384`, `"num_experts_per_tok": 6`
    - "DeepSeek calls it the official release, replacing an earlier preview" + the pulled quote <- card: "DeepSeek-V4-Pro-0813 is the official release of DeepSeek-V4-Pro, superseding the preview version."
  - Reaction ("the open-weights frontier keeps closing on the closed one.") is opinion — asserts no unsourced fact.
  - **Modality guard applied:** weights are actually present and downloadable now (gated:No, 66 shards on the official org) — "published … anyone can now download and run it" is shipped/present-tense true, not an announcement. No total-parameter figure shipped: `config.json` gives no `num_parameters`, and the card model-index metadata ("1.7T") conflicts with Tier-2 secondary reports ("1.6T total / 49B active"), so no param count is claimed — only the config-anchored MoE routing (384 experts / 6 per token). The 1M context and AA Index 53 already posted 13 Aug, deliberately kept out.
- **Not shipped:** OpenAI RSS newest since the 20:00 run = "The builder's guide to GPT-5.6" (13 Aug, guidance) + "OpenAI appoints Dali Rajic as Chief Revenue Officer" (13 Aug, personnel) — non-material, no beat; the 13 Aug Ultrafast preview already POSTED (T17). No net-new 14 Aug OpenAI primary.
- **Model releases:** HF `sort=createdAt` newest 30 (to ~07:1x Rome) = community finetunes / GGUF quants / robotics + eval checkpoints (`mradermacher/*`, `alexkstern/*` nanochat, `RobotisSW/*`) — no *other* official major-lab release. `huggingface.co/Qwen` newest still 8 Aug `Qwen3.8-2.4T-A95B` (+FP8) — no new Qwen3.8 repo dated 13–14 Aug (T16 smaller-model / licence-terms watch-for still open). ByteDance Seed 2.1 Turbo (10 Aug, per an aggregator timeline) has no allowlisted primary rendering — not shipped.
- **Capability shifts:** no *new* independent benchmark that changes standing since 13 Aug — the AA V4-Pro Index 53 already posted; GPT-5.6-Cyber's 95% remains a completion/willingness rate (T12), still no independent eval. HF daily-papers / arxiv cs.AI top items = research (long-horizon agentic design, AI-scientist, speculative diffusion decoding, a 1B "open HRM" — `DFM Mimir v1`) — no major-lab frontier-model card or standing-changing frontier benchmark.
- **Lab feeds:** Anthropic newsroom newest unchanged = 7 Aug "Improving Fable 5's biology safeguards" (T1, already advanced). No new Google DeepMind / Mistral / Meta frontier-LLM primary dated 13–14 Aug.
- **Infrastructure:** no chip/accelerator/serving primary dated 14 Aug beyond the already-posted 13 Aug Ultrafast/Cerebras tier (T17). Anthropic in-house chip-design team (5 Aug) + Meta×BlackRock El Paso capex JV (secondary) remain non-clearing (T4).
- **Regulation/politics:** EU AI Act (T11) — Article 50 + GPAI enforcement live since 2 Aug, **still no named first enforcement action / target / outcome**. No new White House / Commerce / Congress openable primary (T5/T14); Moonshot (T13) no imposed action; Pacing the Frontier (T14) no US-gov reply.
- **Net-new hot topics:** nothing else the field surfaced today clears with a primary + checkable claim.
- **Threads touched:** **T15 ADVANCED (14 Aug 07:00 — DeepSeek-V4-Pro-0813 open weights public on HF, MIT).** Timestamps refreshed elsewhere: T17 (Ultrafast still limited preview, no independent latency/pricing number), T16/Qwen (no new HF repo), T12 (95% still a completion rate), T14/Pacing (no US-gov reply), T13/distillation (no imposed action), T11/EU AI Act (no named first action), T5/gating, T4/silicon, T1 (advanced 7 Aug), T8/T10/T9/T7/T6/T3/T2 quiet or already advanced. **Friday:** weekly take runs after the 20:00 slot (separate routine), not this run.

### Run 13:00 — chosen beat / sources / thread updates

Ran the verified-feed spine + active-thread watch-fors + net-new (~10 lookups) since the 07:00 run (which ADVANCED T15 on DeepSeek-V4-Pro-0813 open weights). Two real candidates surfaced with allowlisted primaries; picked the single most important + clearly-fresh one.

- **CHOSEN (POST — OPEN new thread T18, Z.ai / GLM frontier line):** **Z.ai released GLM-5.3**, its latest flagship coding model, "now available to all GLM Coding Plan users" — built by post-training GLM-5.2 (same base model). The distinctive hook is on offensive security: Z.ai reports GLM-5.3 **84.5% on CyberGym** (a real-world vulnerability-finding benchmark), narrowly above **Anthropic's Mythos 5 (83.8%)** and **OpenAI's GPT-5.6 Sol (83.6%)** — a named-benchmark-number capability claim that clears §2, and a Chinese lab reporting a cyber edge over two US frontier models (inverts the usual T12 direction). Not covered today (07:00 was DeepSeek), not represented by any existing thread → legitimate new POST.
  - **Source-anchored claims:** (anchored to `z.ai/blog/glm-5.3` — allowlisted lab primary; the blog is a JS SPA that returns empty to the fetcher, so shipped via z.ai-scoped WebSearch verbatim + the `docs.z.ai/guides/llm/glm-5.3` overview page, which RENDERS to the fetcher and confirms every claim, per the T7/T12/T16 SPA precedent)
    - "Z.ai released GLM-5.3, its latest flagship coding model, now available to GLM Coding Plan users" <- docs.z.ai: "GLM-5.3 is now available to all GLM Coding Plan users" + "GLM-5.3 is Z.ai's latest flagship model, delivering major advances in complex software engineering and agent tasks"
    - "It's built purely by post-training GLM-5.2: same base model, gains from the later fine-tuning stage" <- "It uses the same base model as GLM-5.2 — all improvements come from post-training."
    - "On CyberGym, a benchmark for finding real-world software vulnerabilities, Z.ai reports it just ahead of Anthropic's Mythos 5 and OpenAI's GPT-5.6 Sol" + the pulled quote <- "GLM-5.3 scores 84.5% on CyberGym, slightly outperforming Mythos 5 at 83.8% and GPT-5.6 Sol at 83.6%." (Mythos 5 = Anthropic, GPT-5.6 Sol = OpenAI — established attributions)
    - "Self-reported; independent results pending" <- numbers appear only on Z.ai's own blog/docs; no zai-org/GLM-5.3 repo on Hugging Face yet (newest is GLM-5.2, 16 Jun), no AA/independent eval — the caveat asserts no fact beyond that
  - **Modality guard applied:** "now available to GLM Coding Plan users" is present-tense true (docs: "GLM-5.3 is now available to all GLM Coding Plan users"). Deliberately NOT claimed: **API availability** (docs: "The GLM-5.3 API is coming soon" — future) and **open weights** (no `zai-org/GLM-5.3` HF repo exists yet, unlike GLM-5/5.1/5.2). A hard-banned aggregator framed the public/weights release as gated pending security testing ("trusted-access system"); I could NOT confirm that from any z.ai primary, so that framing was dropped entirely. No numbers beyond the verbatim CyberGym line shipped (Terminal-Bench 3.0 4.6→28.3, DeepSWE 46.2→66.9, ExploitBench 24.4→54.4, Agents' Last Exam 23.8→28.5 all left in-thread only).
- **Runner-up (NOT shipped — stale + workhorse tier): Google DeepMind Gemini 3.7 Flash.** Real allowlisted primary (`blog.google/.../introducing-gemini-3-7-flash/` + `deepmind.google` model card + `ai.google.dev`), GA, named benchmarks (FrontierCode 43.6% vs 34.4%, DeepSWE 65.3% vs 49.0%) + intro pricing $0.75/$3.75 per M. But **dated 13 Aug** (yesterday) — a workhorse/mid-tier Flash update available since before the 13 Aug 20:00 and 14 Aug 07:00 runs, so not the single most important movement *since the last run*; GLM-5.3 (today, frontier + cyber angle) outranks it for the one-beat-per-slot pick. Logged for a possible future slot if it gains an independent standing-change; not a T-thread yet.
- **Qwen3.8-27B (NOT shipped — not new since 07:00):** the official `Qwen/Qwen3.8-27B` dense 27B (createdAt 5 Aug; FP8 variant 13 Aug) is real, gated:No, and renders on HF — but it predates today and the 07:00 sweep's window, so it's not fresh movement for the 13:00 slot. Leaves the T16 smaller-model watch-for effectively satisfied but not as a 13:00 beat. Aggregator claims of an "Aug 15 open-weights drop" are SEO/aggregator (hard-banned) and unconfirmed on a primary.
- **Other feeds:** OpenAI RSS newest = "The builder's guide to GPT-5.6" (14 Aug, guidance — non-material); no new Anthropic newsroom post (still 7 Aug biology safeguards). HF `sort=createdAt` = community quants/finetunes (many `*/Qwen3.8-27B-*` re-quants) — no *other* official major-lab frontier release. DeepSeek org: no repo past 0813 (already posted 07:00). "GLM-5.3 by Z.ai" was the one net-new frontier primary.
- **Threads touched:** **T18 OPENED + POSTED (14 Aug 13:00 — GLM-5.3 release + CyberGym 84.5% self-report).** Cross-refs T12/T3 (cyber). Timestamps refreshed, no new movement: T15 (advanced 07:00), T17 (Ultrafast preview), T16 (Qwen3.8-27B not new; no new flagship repo), T12 (95% still a completion rate), T11/EU AI Act (no named first action), T14/T13/T5/T4/T1/T10/T9/T8/T7/T6/T2 quiet or already advanced. New net-new note: **Gemini 3.7 Flash (13 Aug GA)** logged as an uncaught prior-day item, not a thread. **Friday:** weekly take runs after the 20:00 slot (separate routine), not this run.

### Run 20:00 — why skipped

Ran the verified-feed spine + active-thread watch-fors + net-new (~9 lookups) since the 13:00 run (which OPENED + POSTED T18 on Z.ai's GLM-5.3). Nothing cleared the materiality gate — the flagged US-lab evening window produced no net-new frontier primary.

- **Both of today's real movements already posted:** DeepSeek-V4-Pro-0813 open weights (07:00, T15 ADVANCE) and GLM-5.3 + CyberGym 84.5% self-report (13:00, T18 OPEN+POST). Re-posting either would be a same-day rehash → barred by §2 dedup.
- **US-lab feeds checked, nothing new since the last run:** OpenAI RSS newest still 13 Aug ("The builder's guide to GPT-5.6" / Ultrafast preview / Dali Rajic CRO) — no 14 Aug primary; Anthropic newsroom unchanged (still 7 Aug "Improving Fable 5's biology safeguards"); Google DeepMind blog newest still Gemini 3.7 Flash (13 Aug). An Anthropic × SpaceX "300MW / 220k GPU" compute item surfaced in search but the primary (`anthropic.com/news/higher-limits-spacex`) is dated **6 May 2026** — old, not a candidate.
- **Gemini 3.7 Flash (NOT shipped — prior-day mid-tier, standing-neutral):** already logged as a 13 Aug runner-up at 13:00. Its independent AA eval is now live (`artificialanalysis.ai` — Intelligence Index 56 high / 53 medium / 51 low, median 34), but a mid-tier Flash at 56 sits below GPT-5.6 Sol (59) / Opus 5 & Fable 5 (60–61) — a speed/price-Pareto story, not a frontier standing change. Not the single most important movement of the slot; doesn't clear as a fresh beat.
- **Model releases:** HF `sort=createdAt` newest 30 = community quants/finetunes (many `*/Qwen3.8-27B-*` re-quants, `alexkstern/*` nanochat, `Affine-*` checkpoints) — no *other* official major-lab frontier release; no `zai-org/GLM-5.3` repo yet (T18 open-weights watch-for still open); no new `deepseek-ai`/`Qwen` flagship repo past what's already posted.
- **Capability shifts:** no *new* independent benchmark that changes frontier standing since 13:00 — no independent GLM-5.3 (CyberGym 84.5% still self-reported), no independent DeepSeek-V4-Pro-0813 open-weights eval, GPT-5.6-Cyber's 95% still a completion rate (T12).
- **Infrastructure / Regulation / net-new:** no chip/serving primary past the 13 Aug Ultrafast/Cerebras tier (T17); EU AI Act (T11) still no named first enforcement action; Moonshot distillation (T13) no imposed action; Pacing the Frontier (T14) no US-gov reply; nothing else the field surfaced tonight clears with a primary + checkable claim.
- **Threads touched:** none advanced. Timestamps refreshed on THREADS.md; no material change since 13:00. **Friday:** the weekly take is a separate routine (`routines/take.md`), not this daily-slot run.

## Telegram — Run 13:00

Z.ai released GLM-5.3, its latest flagship coding model, now available to GLM Coding Plan users. It's built purely by post-training GLM-5.2: same base model, gains from the later fine-tuning stage.

On CyberGym, a benchmark for finding real-world software vulnerabilities, Z.ai reports it just ahead of Anthropic's Mythos 5 and OpenAI's GPT-5.6 Sol. Self-reported; independent results pending.

"GLM-5.3 scores 84.5% on CyberGym, slightly outperforming Mythos 5 at 83.8% and GPT-5.6 Sol at 83.6%."

#GLM #ZAI #LLM #Cyber

https://z.ai/blog/glm-5.3

## Telegram — Run 20:00

SKIPPED — no material movement since 13:00. Both of today's real beats already shipped (DeepSeek-V4-Pro-0813 open weights, 07:00 T15; GLM-5.3, 13:00 T18). No net-new US-lab primary this evening; Gemini 3.7 Flash is a 13 Aug mid-tier workhorse (now independent AA Index 56 high — standing-neutral), not fresh movement.
