# AI/LLM Briefing — 20 August 2026

## Telegram — Run 07:00

SKIPPED — no material movement since 19 Aug 20:00. (No first-party frontier model card dated 19–20 Aug — HF new-models API is community quants/finetunes/test repos only, no first-party deepseek-ai/Qwen/zai-org/meta/google/openai/xai/mistralai/moonshotai/anthropic repo; Anthropic newsroom unchanged (14 Aug watermark), Google DeepMind newest still Gemini 3.7 Flash (13 Aug). OpenAI RSS newest items are the 19 Aug "Zero Data Retention" (privacy/API) and "Replit × GPT-5.6 Luna" (partner/product) — both non-clearing, already noted 19 Aug. No new independent/AA article since Grok 4.6 / Gemini 3.7 Flash; AA "Optima" (18 Aug) is a benchmarking platform, not a result; DeepSeek/Qwen "independent" numbers surface only on aggregator/SEO blogs. GLM-5.3 (T18) still gated on HF. No new regulation primary. No active thread crossed its Watch for.)

## Telegram — Run 13:00

SKIPPED — no material movement since 07:00. (No new first-party frontier model card 20 Aug — HF new-models API is community quants/finetunes/TestRepos only; OpenAI RSS unchanged (newest still 19 Aug ZDR + Replit/Luna); Anthropic newsroom unchanged (14 Aug watermark); Google DeepMind newest still Gemini 3.7 Flash. AA shipped only an Intelligence Index v4.1.1 methodology patch, no new model result (Opus 5 still #1 at 63). GLM-5.3 (T18) open weights still not public — no `zai-org/GLM-5.3` repo (newest is GLM-5.2, 16 Jun), staged release still ~28 Aug. Astra RL pause (T12) already posted 19 Aug — no resume/next-model news, no gov/AISI response. No new regulation primary. No active thread crossed its Watch for.)

## Notes (archive only — not sent to Telegram)

### Run 07:00 — why skipped

First slot of the day (Thursday). Ran the verified-feed spine (HF new-models API, OpenAI RSS, Anthropic newsroom) + all active-thread watch-fors + a net-new sweep (~9 lookups) since the 19 Aug 20:00 SKIP. Last POST/ADVANCE was 19 Aug 07:00 (T12/T14 — OpenAI two-week RL pause / Astra-may-be-Critical). Nothing cleared the materiality gate:

- **No first-party frontier model card dated 19–20 Aug.** HF `sort=createdAt&direction=-1&limit=40` = community finetunes/quants/test repos only (`mradermacher` GGUFs, `qtum/Qwen3.8-27B-GGUF` + other Qwen3.8-27B quants, `juwon1105` RLVR/RLCC training steps, dozens of `*TestRepo`/`MyAwesomeModel` repos) — no first-party `deepseek-ai`/`Qwen`/`zai-org`/`meta`/`google`/`openai`/`xai`/`mistralai`/`moonshotai`/`anthropic` repo. Anthropic newsroom unchanged (newest 14 Aug watermark); Google DeepMind newest still Gemini 3.7 Flash (13 Aug).
- **OpenAI RSS:** newest items are the 19 Aug "Offering Zero Data Retention for frontier models" (data-privacy/API feature) and "Replit expands access … with GPT-5.6 Luna" (partner/product) — both already noted in the 19 Aug 20:00 SKIP, neither a model card / infra shift / benchmarked capability number / gov primary → non-clearing. All older items (18 Aug Pacing, ChatGPT Ads, ChatGPT for Teens, etc.) already handled.
- **Capability shifts:** no new independent/AA article since Grok 4.6 (12 Aug) / Gemini 3.7 Flash (13 Aug). AA's 18 Aug "Optima" is a *platform* for benchmarking on your own workloads, not a model result → non-clearing. DeepSeek-V4-Pro-0813 (T15) and Qwen3.8 (T16) "independent" numbers surface only on aggregator/SEO blogs (codersera, morphllm, techjacksolutions) — one even misattributes Claude Opus 5's SWE-bench Verified 96.4% to DeepSeek — none an allowlisted specialist primary, so no independent-eval beat clears. GLM-5.3 (T18) still self-reported (CyberGym 84.5%); `zai-org/GLM-5.3` HF repo still created-but-gated, no downloadable weights (staged open release still ~late Aug per secondary press).
- **Regulation/politics:** EU AI Act (T11) — Article 50 + GPAI enforcement live since 2 Aug; first enforcement actions still expected Q4 2026 (no named target/outcome). White House framework (T5) still the 2 June EO instrument, no new whitehouse.gov/Commerce/Congress/NIST openable primary. Moonshot distillation (T13) no imposed action; Pacing the Frontier employee statement (T14) no US-gov reply.
- **Cyber/pacing (T12):** the 18 Aug "Pacing" post + two-week RL pause / Astra-may-be-Critical was already POSTED 19 Aug 07:00; no finalized Astra Critical designation, no RL-resume/next-model news, no gov/AISI response primary.
- **Threads touched:** timestamps refreshed on all; no thread crossed its Watch for. No POST. Thursday — no weekly take (Friday-only routine).

### Run 13:00 — why skipped

Second slot (Thursday). Re-ran the verified-feed spine (HF new-models API, OpenAI RSS, Anthropic newsroom, Google DeepMind blog) + AA + active-thread watch-fors + a net-new sweep (~7 lookups) since the 07:00 SKIP. Last POST/ADVANCE was 19 Aug 07:00 (T12/T14). Nothing cleared the materiality gate:

- **No first-party frontier model card 20 Aug.** HF `sort=createdAt&direction=-1&limit=40` = community finetunes/quants/`MyAwesomeModel-TestRepo` spam only — no first-party `deepseek-ai`/`Qwen`/`zai-org`/`meta`/`google`/`openai`/`xai`/`mistralai`/`moonshotai`/`anthropic` repo. OpenAI RSS newest unchanged (19 Aug "Zero Data Retention" + "Replit × GPT-5.6 Luna", both non-clearing, noted 19–20 Aug). Anthropic newsroom newest still 14 Aug watermark. Google DeepMind newest still Gemini 3.7 Flash (13 Aug).
- **Capability shifts:** Artificial Analysis shipped only an **Intelligence Index v4.1.1** update — a *methodology* patch (upgraded graders + τ³-Banking v1.0.1; most models moved <1 pt, largest Muse Spark 1.2 +2.7), NOT a new model result; Claude Opus 5 still #1 at 63 → non-clearing. No new independent/AA model article since Grok 4.6 (12 Aug) / Gemini 3.7 Flash (13 Aug). DeepSeek-V4-Pro-0813 (T15) / Qwen3.8 (T16) independent numbers still only on aggregator/SEO blogs — none an allowlisted specialist primary.
- **GLM-5.3 (T18):** open weights still not public — no `zai-org/GLM-5.3` repo (newest zai-org is GLM-5.2, 16 Jun); staged open release still expected ~28 Aug per secondary press; CyberGym 84.5% remains self-reported. Watch-for uncrossed.
- **Cyber/pacing (T12):** the 18 Aug two-week RL pause / Astra-may-be-Critical was POSTED 19 Aug 07:00; only aggregator rehashes since — no finalized Astra Critical designation, no RL-resume/next-model news, no gov/AISI response primary.
- **Regulation/politics:** EU AI Act (T11) Article 50 + GPAI enforcement live since 2 Aug, first actions still Q4 2026 (no named target). No new whitehouse.gov/Commerce/Congress/NIST openable primary (T5); Moonshot distillation (T13) no imposed action; Pacing the Frontier statement (T14) no US-gov reply.
- **Threads touched:** timestamps refreshed on all; no thread crossed its Watch for. No POST/ADVANCE. Thursday — no weekly take.
