# AI/LLM Briefing — 16 August 2026

## Telegram — Run 07:00

SKIPPED — no material movement since the 15 Aug 20:00 run. (No official-lab model card dated 15–16 Aug; OpenAI RSS newest 13 Aug, Anthropic newest 14 Aug. DeepSeek's API price hike (T15) doesn't take effect until 16:00 UTC today — announced/pending, not live, so no ADVANCE this slot; it crosses this evening. GLM-5.3 still no open weights + self-reported only (T18); no new independent benchmark changing standing (T15/T18/T12/T19).)

## Telegram — Run 13:00

SKIPPED — no material movement since 07:00. (DeepSeek's API price hike (T15) still doesn't take effect until 16:00 UTC = 18:00 Rome — announced/pending, not live, so still no ADVANCE; it crosses this evening. No new official-lab model card 15–16 Aug; HF new-models list is community re-uploads/quants only. GLM-5.3 open weights still ~2 weeks out + self-reported only (T18); no new independent benchmark changing standing; no Gemini Pro/Ultra tier (T19); no fresh gov/regulation primary.)

## Notes (archive only — not sent to Telegram)

### Run 07:00 — why skipped

First slot of the day (Sunday). Ran the verified-feed spine + active-thread watch-fors + net-new (~9 lookups across the five categories) since the 15 Aug 20:00 run (a POST — opened T19, Gemini 3.7 Flash, independent AA Intelligence Index 56). Nothing cleared §2.

- **No official major-lab model release dated 15–16 Aug.** HF `sort=createdAt&direction=-1&limit=40` = community finetunes/quants only (`longtermrisk/*`, `ERISLab/*`, `unsloth/*` re-uploads) — no `deepseek-ai` / `Qwen` / `zai-org` / `meta` / `google` / `openai` / `xai` / `mistralai` / `moonshotai` repo. OpenAI RSS unchanged (newest 13 Aug — Ultrafast preview / builder's guide / Dali Rajic CRO). Anthropic newsroom unchanged (newest 14 Aug watermark, posted 15 Aug 07:00).
- **DeepSeek API price hike (T15 watch-for) — pending, not live.** Confirmed via search: the new peak/off-peak pricing takes effect **16:00 UTC on 16 Aug 2026** (= 18:00 Rome; peak hours 01:00–04:00 & 06:00–10:00 UTC, off-peak at half rate; increases +50%→>1,100% by model/token-type/hour). At 07:00 Rome (05:00 UTC) it is **announced but NOT yet in effect** → modality rule (§5) forbids posting it as live, so no ADVANCE this slot. It crosses at 18:00 Rome today — a genuine ADVANCE candidate for the 20:00 run once `api-docs.deepseek.com/quick_start/pricing` shows the new numbers.
- **Capability shifts:** no *new* independent benchmark changing frontier standing since 15 Aug. GLM-5.3 (T18) still self-reported (CyberGym 84.5%) with **no open-weights repo** on `huggingface.co/zai-org` (newest still `GLM-5.2`, 16 Jun; secondary press now dates the staged open-weights drop ~28 Aug, delayed for security hardening) — watch-for still open, no change. DeepSeek-V4-Pro-0813 open-weights build (T15) still lacks an independent eval; GPT-5.6-Cyber's 95% (T12) remains a completion/willingness rate. No Gemini Pro/Ultra tier — T19's Flash already posted.
- **Infrastructure:** no chip/accelerator/serving primary dated 15–16 Aug beyond the already-posted 13 Aug Ultrafast/Cerebras tier (T17, still limited preview). Anthropic in-house chip-design team (5 Aug, secondary) still non-clearing (T4).
- **Regulation/politics:** EU AI Act (T11) — Article 50 + GPAI enforcement live since 2 Aug; first *enforcement actions* still expected Q4 2026 (no named target/outcome). Colorado's AI-chatbot-for-minors law surfaced in weekend secondary press but has no fresh allowlisted primary opened this session and is US-state, not a briefing-clearing gov primary. No new White House / Commerce / Congress openable primary (T5/T14); Moonshot distillation (T13) no imposed action; Pacing the Frontier (T14) no US-gov reply.
- **Net-new hot topics:** nothing else the field surfaced this weekend clears with a primary + checkable claim.
- **Threads touched:** none advanced. Timestamps refreshed on all; **T15 note updated** (price hike still dated 16 Aug 16:00 UTC, pending — crosses this evening). No other movement since the 15 Aug 20:00 run (T1/T7/T19 already advanced/opened yesterday; T18/T17/T16/T12/T14/T13/T11/T5/T4/T10/T9/T8/T6/T3/T2 quiet or already advanced). Sunday — no weekly take (Friday-only routine).

### Run 13:00 — why skipped

Ran the verified-feed spine + active-thread watch-fors + net-new (~6 lookups) since the 07:00 SKIP. Nothing cleared §2 — same quiet Sunday.

- **DeepSeek API price hike (T15) — still pending, not live.** Re-confirmed: the peak/off-peak switch takes effect **16:00 UTC (= 18:00 Rome)**; at 13:00 Rome (11:00 UTC) it is announced-but-not-in-effect → modality rule (§5) still forbids posting it as live, so no ADVANCE. Named numbers now sharpened (V4-Pro output $0.87 → $1.98 off-peak / $3.96 peak per 1M; input cache-miss $0.435 → $0.66 / $1.32; V4-Flash output $0.28 → $0.66 / $1.32) — held for the 20:00 run, which is when it crosses, anchored to `api-docs.deepseek.com/quick_start/pricing` once the new numbers render live.
- **No new official-lab release 15–16 Aug.** HF `sort=createdAt&direction=-1&limit=40` = community re-uploads/quants only (`VantoraLabs/*`, `taurusduan/GLM-5.2-Vision-*`, `taurusduan/DeepSeek-V4-Flash-*-Vision`, `gabrielcosi/Muse-Glimmer-30B-*-GGUF`, `KENIC-1/Qwen3.8-27B-GGUF`) — no first-party `deepseek-ai` / `Qwen` / `zai-org` / `meta` / `google` / `openai` / `xai` / `mistralai` / `moonshotai` repo. OpenAI RSS unchanged (newest 13 Aug). Anthropic newsroom unchanged (newest 14 Aug watermark).
- **Capability shifts:** no new independent benchmark since 15 Aug. GLM-5.3 (T18) weights confirmed still ~2 weeks out (secondary press: staged ~28 Aug after safety hardening), self-reported only. No Gemini Pro/Ultra tier (T19). AA top unchanged (Opus 5 at 63).
- **Regulation/politics:** no fresh allowlisted gov primary (T5/T11/T13/T14). Quiet.
- **Threads touched:** none advanced. All timestamps refreshed; T15 note re-affirmed (crosses 18:00 Rome — the 20:00 candidate).
