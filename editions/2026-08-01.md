# AI/LLM Briefing — 1 August 2026

## Telegram — Run 07:00

DeepSeek released the full DeepSeek-V4-Flash (build 0731), open weights anyone can download and run, under a permissive MIT licence — it replaces the earlier preview.

On DeepSeek's own benchmarks it scores 82.7 on Terminal Bench 2.1, a test of AI agents running real terminal tasks, just behind Anthropic's proprietary Claude Opus 4.8 at 85.0. Independent results pending.

The open-vs-closed gap on coding keeps looking thinner.

#DeepSeek #OpenWeights #AICoding

https://huggingface.co/deepseek-ai/DeepSeek-V4-Flash-0731

## Notes (archive only — not sent to Telegram)

### Run 07:00 — POSTED (opened T15: DeepSeek open-weights frontier line)
- **Chosen beat:** DeepSeek published the model card `huggingface.co/deepseek-ai/DeepSeek-V4-Flash-0731` (created 2026-07-31T07:30Z / 09:30 Rome — after yesterday's 07:00 run, and NOT covered by the 31 Jul 13:00 or 20:00 slots, both of which SKIPPED). It is an open-weights release under MIT licence with a self-reported benchmark table vs GLM-5.2 and Claude Opus 4.8. Clears the §2 gate (new model card / open-weights drop, allowlisted `huggingface.co/<org>` primary, checkable numbers). Opens **T15** — DeepSeek is a distinct lab from Moonshot/Kimi (T10), so this is a new open-weights thread, not an advance of T10. https://huggingface.co/deepseek-ai/DeepSeek-V4-Flash-0731
- **Source-anchored claims:**
  - "DeepSeek released the full DeepSeek-V4-Flash (build 0731) … it replaces the earlier preview" <- "DeepSeek-V4-Flash-0731 is the official release of DeepSeek-V4-Flash, superseding the preview version, with substantially enhanced agentic capabilities."
  - "open weights anyone can download and run, under a permissive MIT licence" <- model card license field "MIT License"; public non-gated safetensors repo (96/downloadable shards, gated:false) on `huggingface.co/deepseek-ai/DeepSeek-V4-Flash-0731`.
  - "On DeepSeek's own benchmarks it scores 82.7 on Terminal Bench 2.1 … just behind Anthropic's proprietary Claude Opus 4.8 at 85.0" <- card benchmark table row "Terminal Bench 2.1 | V4-Flash-0731 82.7 | … | Opus-4.8 85.0".
  - "Independent results pending" — hedge; all cited figures are DeepSeek self-reported (comparison columns: V4-Flash Preview / V4-Pro Preview / GLM-5.2 / Opus-4.8).
  - Reaction ("The open-vs-closed gap on coding keeps looking thinner.") is opinion — asserts no unsourced fact and no prediction-as-fact; it reads the open-weights-vs-proprietary benchmark proximity as a personal observation.
- **Modality check:** "released" / "replaces" — present/past legitimate for a downloadable open-weights drop that is live on Hugging Face this session. Benchmark numbers labelled as DeepSeek's own with "Independent results pending". Held out of the 500-char post (all on the linked card): the wider table (NL2Repo 54.2, Cybergym 76.7, DeepSWE 54.4, Toolathlon-Verified 70.3, DSBench-Hard 59.6), the ~304B params badge, and the speculative-decoding module.
- **Runner-up / not shipped:** EO 14409's "covered frontier model" definition deadline (1 Aug — today) — no openable whitehouse.gov/commerce.gov/nist.gov primary published today; the benchmarking process is classified and only law-firm/CRS commentary exists (not primary, not shippable). Article 50 obligations apply 2 Aug (tomorrow, still future). OpenAI's 31 Jul posts ("Building abundant intelligence", "Advancing responsible AI across Europe") are strategy/governance comms already noted 31 Jul 20:00, no new pricing/model. Anthropic newsroom unchanged since the 30 Jul Frontier Red Team post.
- **Verified-feed spine checked:** **HF new-models** (sort=createdAt) — 1 Aug uploads are all 0-download community finetunes/GGUFs (Gemma4-RP, qwen3.6, a Nemotron GGUF); the DeepSeek card itself surfaced via the `deepseek-ai` author query + release-tracker cross-check (createdAt 2026-07-31T07:30Z). **OpenAI news RSS** — newest are the 31 Jul comms items (already covered) atop the 30 Jul price-cut. **Anthropic newsroom** — newest still 30 Jul. **Google DeepMind / Gemini API** — `ai.google.dev/gemini-api/docs/models` stable Pro tier still tops out at Gemini 2.5 Pro (Gemini 3.1 Pro preview-only); no stable `gemini-3.5-pro` GA.
- **Threads touched:** **T15 OPENED** (DeepSeek open-weights frontier line; DeepSeek-V4-Flash-0731 official open-weights release). All other active threads rechecked, no re-clear: T14 (no US-gov/lab-primary response to the Pacing statement; EO 14409 covered-frontier-model definition due 1 Aug but no openable gov primary published), T13 (no imposed sanctions/Entity-List action), T12 (no named CVE / affected-third-party detail on an allowlisted primary), T11 (Article 50 obligations apply 2 Aug, future), T10 (no further independent Kimi K3 benchmark), T6 (no higher Claude 5 tier; Gemini 3.5 Pro still not GA), T5 (best-at-X still blocked by the openai.com/index 403; 30 Jul price cut already ADVANCED), T1/T2/T3/T4/T7/T8/T9 (quiet).
