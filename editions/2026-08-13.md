# AI/LLM Briefing — 13 August 2026

## Telegram — Run 07:00

DeepSeek's top-tier model shows a new build in its API price list — DeepSeek-V4-Pro-0813 — with a 1-million-token context window (how much text it can read at once).

It stays cheap: about $0.44 per million input tokens on a cache miss, and $0.87 per million output. But DeepSeek warns that's about to change.

"We plan to raise the overall pricing for DeepSeek API services in the near future, with a significant increase expected."

the price-rise warning may be the real story here.

#DeepSeek #LLM #Pricing

https://api-docs.deepseek.com/quick_start/pricing

## Notes (archive only — not sent to Telegram)

### Run 07:00 — chosen beat / sources / thread updates

First slot of the day (Thursday). Ran the verified-feed spine + active-thread watch-fors + net-new (9 lookups across the five categories) since the 12 Aug 20:00 run (which ADVANCED T16 — Qwen3.8 open weights on Hugging Face).

- **CHOSEN (ADVANCE T15 — DeepSeek open-weights frontier line):** DeepSeek's flagship **V4-Pro** tier now runs a new build, **DeepSeek-V4-Pro-0813**, live behind the `deepseek-v4-pro` API endpoint and listed on the DeepSeek API pricing page (`api-docs.deepseek.com/quick_start/pricing`) with prices and specs. This is the exact T15 Watch-for — "the DeepSeek-V4-Pro full (non-preview) GA release" — which was still unmet at the 12 Aug 20:00 run (DeepSeek graduated the smaller V4-Flash to official status on 31 Jul and said in its changelog the Pro's official release "will follow soon"; no GA date had been announced). DeepSeek shipped it without a blog post or tweet — the build surfaced only in the API docs (the docs home carries a verbatim note, "The `deepseek-v4-pro` model has been updated to DeepSeek-V4-Pro-0813"). The pricing page is an **allowlisted primary that renders to the fetcher** — no scoped-search workaround needed. Last POSTED on T15 was the 1 Aug 20:00 AA eval of V4-Flash-0731 — distinct beat (Pro-tier GA + pricing, not the Flash benchmark), so a legitimate ADVANCE, not a repeat.
  - **Source-anchored claims:** (all anchored to `api-docs.deepseek.com/quick_start/pricing`, opened this session)
    - "DeepSeek's top-tier model shows a new build in its API price list — DeepSeek-V4-Pro-0813" <- pricing page lists the model version "DeepSeek-V4-Pro-0813" for the `deepseek-v4-pro` endpoint (Pro is the higher tier vs `deepseek-v4-flash`)
    - "with a 1-million-token context window" <- pricing-page spec cell: context length "1M" for `deepseek-v4-pro` (max output "384K")
    - "about $0.44 per million input tokens on a cache miss, and $0.87 per million output" <- pricing page: input cache-miss $0.435, output $0.87 per 1M tokens
    - "But DeepSeek warns that's about to change." + the verbatim quote <- pricing-page notice: "We plan to raise the overall pricing for DeepSeek API services in the near future, with a significant increase expected."
  - Reaction ("the price-rise warning may be the real story here.") is opinion — asserts no unsourced fact.
- **Not shipped (deliberately kept out of the single-URL post):** the secondary/Tier-2 architecture + capability details around the 0813 build (Unite.AI / Decrypt / digitaltoday report 1.6T total / 49B active MoE, and "Fable 5 ~5% better at ~46× the price" on nine shared items) — no primary DeepSeek benchmark card for the 0813 build renders (DeepSeek published no blog), and those numbers aren't on the pricing page, so they don't ship. The 0813 GA **weights are not on Hugging Face** yet (`huggingface.co/deepseek-ai` newest by createdAt still 31 Jul `DeepSeek-V4-Flash-0731`; the only V4-Pro repo is the 4 Jul `DeepSeek-V4-Pro-DSpark` preview module) — so the "open weights" angle can't ship and isn't claimed; this run's beat is the API/pricing GA only.
- **Model releases:** HF `sort=createdAt` newest 30 (to ~07:1x Rome) = community finetunes / GGUF quants / test repos / Keras-format `qwen3-*` reimplementations / ASR (Whisper) variants — no official major-lab release. **`huggingface.co/Qwen` — Qwen3.8-27B open weights still not landed** (T16; the promised smaller-model drop still has no HF repo / card / licence). **`huggingface.co/deepseek-ai` newest still 31 Jul `DeepSeek-V4-Flash-0731`** (the 0813 Pro GA is API-only so far, per above).
- **Capability shifts:** one secondary claim of a fresh AA number for Qwen3.8-Max (Intelligence Index "56" at ~$1.14/task via a DataCamp/BenchLM roundup) — but no AA article or model page renders to the fetcher to confirm it, and it conflicts with the 5 Aug AA Index 53 already logged; not shipped (T16 watch-for for an *independent* open-weights benchmark remains anchored to an openable AA primary). GPT-5.6-Cyber's 95% remains a completion/willingness rate (T12), still no independent eval.
- **Lab feeds:** OpenAI RSS newest = 12 Aug enterprise-adoption research ("From assistance to execution") + RingCentral case study — non-material business/PR; the 11 Aug "Daybreak models now available on AWS" / "Testing ads in ChatGPT" already logged. Anthropic newsroom newest unchanged = 7 Aug "Improving Fable 5's biology safeguards" (T1, already advanced). No new Google DeepMind / Mistral / Meta frontier-LLM primary dated 12–13 Aug.
- **Infrastructure:** no chip/accelerator/serving primary dated 12–13 Aug. Anthropic in-house chip-design team (5 Aug) + Meta×BlackRock El Paso capex JV (secondary) remain non-clearing, logged under T4.
- **Regulation/politics:** EU AI Act (T11) — Article 50 + GPAI enforcement live since 2 Aug, **still no named first enforcement action / target / outcome**. No new White House / Commerce / Congress openable primary (T5/T14); Moonshot (T13) no imposed action; Pacing the Frontier (T14) no US-gov reply.
- **Threads touched:** **T15 ADVANCED (13 Aug 07:00 — DeepSeek-V4-Pro-0813 GA + pricing).** Timestamps refreshed elsewhere: T16/Qwen (27B weights not landed; unconfirmed AA "56"), T12 (95% still a completion rate, no independent eval), T14/Pacing (no US-gov reply), T13/distillation (no imposed action), T11/EU AI Act (no named first action), T5/gating (framework/EO pending a primary), T4/silicon, T1 (advanced 7 Aug), T8 (advanced 10 Aug), T10/T9/T7/T6/T3/T2 quiet or already advanced.

### Run 13:00 — chosen beat / sources / thread updates

Second slot. Ran the verified-feed spine + active-thread watch-fors + net-new since the 07:00 run (which ADVANCED T15 on the DeepSeek-V4-Pro-0813 GA + pricing, "independent results pending"). 6 lookups.

- **CHOSEN (ADVANCE T15 — DeepSeek open-weights frontier line):** the first **independent** benchmark of `DeepSeek-V4-Pro-0813` landed on Artificial Analysis — **Intelligence Index 53**. This is the exact T15 Watch-for ("an *independent* benchmark of `DeepSeek-V4-Pro-0813` (AA Intelligence Index / a named coding leaderboard) that changes standing"), and the 07:00 GA beat explicitly shipped the card/pricing as self-reported with "independent results pending." Distinct fact from the morning's API/pricing GA → legitimate ADVANCE, not a repeat. The AA model page `artificialanalysis.ai/models/deepseek-v4-pro` **renders to the fetcher** (allowlisted specialist primary) and states the score + pricing verbatim.
  - **Source-anchored claims:** (all anchored to `artificialanalysis.ai/models/deepseek-v4-pro`, opened this session)
    - "Artificial Analysis … scores DeepSeek's new top-tier model — DeepSeek-V4-Pro-0813 — at 53 on its Intelligence Index" <- AA page verbatim: "DeepSeek V4 Pro 0813 (Reasoning, Max Effort) scores 53 on the Artificial Analysis Intelligence Index"
    - "about $0.43 per million input tokens, and $0.87 per million output" <- AA page pricing: "$0.43 per 1M input tokens", "$0.87 per 1M output tokens"
    - pulled quote is that AA sentence, verbatim.
  - Reaction ("a flagship priced like a budget model — DeepSeek's whole strategy in one line.") is opinion about positioning; the only fact it leans on (the cheap price) is sourced above.
- **Deliberately NOT shipped — drifted peer numbers.** AA's own comparison pages (via AA-scoped search) now render `V4-Flash-0731` at **52** (our 31 Jul receipt logged the AA article's **50**) and `Kimi K3 (max)` at **60** (T10 logged **57**) — AA has evidently recalibrated the index since those posts. Because these peer numbers have moved, the beat cites **only** the AA V4-Pro page's own score (53) + pricing, and makes **no** competitor-comparison claim. SCMP's "four points behind mid-tier GPT-5.6 Terra / on par with GLM-5.2 (53)" framing is Tier-2-ish and unallowlisted → not shipped.
- **Model releases:** HF `sort=createdAt` newest 30 (to ~11:2x UTC) = community GGUF quants / finetunes / test repos (`GLM5.2-fp8`, `Qwen3.6-27B-Jormungandr-GGUF`, REFFT tinies) — **no official major-lab release** dated 13 Aug. `huggingface.co/Qwen` Qwen3.8-27B open weights **still not landed** (T16); `huggingface.co/deepseek-ai` newest still 31 Jul `DeepSeek-V4-Flash-0731` (the 0813 Pro GA remains **API-only**, no HF weights).
- **Capability shifts:** the only fresh benchmark since 07:00 is the AA V4-Pro-0813 = 53 (shipped). Qwen3.8-Max AA number still inconsistent across secondary roundups (53 logged vs "56"/"58" claimed) with no AA article/model-page confirming a change → T16 unshipped. GPT-5.6-Cyber 95% still a completion rate (T12), no independent eval.
- **Lab feeds:** OpenAI RSS unchanged since 07:00 (newest 12 Aug enterprise-adoption + RingCentral, non-material). Anthropic newsroom unchanged, newest 7 Aug biology safeguards (T1). No new Google DeepMind / Mistral / Meta frontier-LLM primary dated 12–13 Aug (the 5 Aug DeepMind leadership reshuffle is old + secondary). arxiv newest batch (12 Aug) = research papers, no frontier release.
- **Infrastructure:** no chip/accelerator/serving primary dated 12–13 Aug (T4 quiet).
- **Regulation/politics:** EU AI Act (T11) — enforcement live since 2 Aug, **still no named first enforcement action / target / outcome**. No new White House / Commerce / Congress openable primary (T5/T14); Moonshot (T13) no imposed action; Pacing the Frontier (T14) no US-gov reply.
- **Threads touched:** **T15 ADVANCED (13 Aug 13:00 — first independent AA benchmark, Intelligence Index 53).** Timestamps refreshed elsewhere: T16/Qwen (27B weights not landed; AA number still unconfirmed), T12, T14, T13, T11, T5, T4, T1, T8, T10/T9/T7/T6/T3/T2 quiet or already advanced.

## Telegram — Run 13:00

Artificial Analysis, an independent firm that runs its own tests, scores DeepSeek's new top-tier model — DeepSeek-V4-Pro-0813 — at 53 on its Intelligence Index.

The Intelligence Index blends many benchmarks into one number. The build stays cheap: about $0.43 per million input tokens, $0.87 per million output.

"DeepSeek V4 Pro 0813 (Reasoning, Max Effort) scores 53 on the Artificial Analysis Intelligence Index"

a flagship priced like a budget model — DeepSeek's whole strategy in one line.

#DeepSeek #LLM #Benchmarks

https://artificialanalysis.ai/models/deepseek-v4-pro

## Telegram — Run 20:00
