# MODELS.md — "Best at what" — refreshed _pending first run_ (weekly)

> **Opinion with receipts.** Every ranking below must cite a **named benchmark + a
> number + a date + a primary URL**. Benchmarks are gameable and often self-reported —
> treat them as evidence, not verdict. Self-reported numbers are flagged; independent /
> reproducible results are preferred. Refreshed weekly by `routines/models.md` only.
> Daily briefing runs never edit this file's rankings.

_This is a skeleton. The first weekly refresh fills every row with current,
sourced entries. An entry with no benchmark + number + date + primary URL does
not belong here — drop it rather than guess._

---

## Best at coding
1. _Model_ — _benchmark_ (e.g. SWE-bench Verified) _NN%_ · _date_ · [source]
   - Caveat: _self-reported vs independent · harness / contamination notes_

## Best at reasoning
1. _Model_ — _named eval_ (e.g. GPQA Diamond / AIME) _NN%_ · _date_ · [source]

## Longest usable context
1. _Model_ — _stated window_ AND a _third-party long-context eval result_ · _date_ · [source]
   - Note: stated window ≠ usable window; cite an eval, not just the spec sheet.

## Best multimodal (vision / audio)
1. _Model_ — _named eval_ _result_ · _date_ · [source]

## Best agentic / tool use
1. _Model_ — _named eval_ (e.g. tau-bench / SWE-bench agentic) _result_ · _date_ · [source]

## Cheapest capable
1. _Model_ — _$ per million input/output tokens_ · _date_ · [official pricing page]

## Best open-weight
1. _Model_ — _license_ · _headline benchmark + number_ · _date_ · [model card]

## Fastest (throughput)
1. _Model on provider/hardware_ — _tokens/sec_ · _date_ · [source]

---

## Methodology & limits
- Every claim carries a named benchmark, a number, a date, and a primary URL opened during the refresh.
- Self-reported numbers are labelled; third-party / reproducible evals are preferred.
- "Best" is a snapshot, not a law — models leapfrog weekly. The date on each row is load-bearing.
- Last full refresh: _pending_. Next scheduled: weekly (Sunday 18:00 Europe/Rome).
