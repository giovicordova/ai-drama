# AI/LLM Briefing — 12 July 2026

## Telegram — Run 07:00

SKIPPED — no material movement since 11 Jul 20:00. No new major-lab model card (HF newest is community Qwen/miniclay uploads; 12 Jul is Sunday, arxiv newest still the 09 Jul weekday batch); OpenAI, Anthropic, and Google DeepMind blogs all still show their pre-10 Jul items — no 10–12 Jul release primary; GPT-5.6 best-at-X still blocked (`openai.com/index/gpt-5-6` still 403s); Gemini 3.5 Pro GA still reported-only (~17 Jul); T1 quiet pre-14-Jul filing; all eight active threads rechecked, none crossed its Watch-for.

## Telegram — Run 13:00

SKIPPED — no material movement since 07:00. No new major-lab model card (HF newest still community Qwen/Gemma finetunes; 12 Jul is Sunday, arxiv newest still the 09 Jul weekday batch); OpenAI, Anthropic, and Google DeepMind blogs all still show their pre-10 Jul release items — no 10–12 Jul release primary; GPT-5.6 best-at-X still blocked (`openai.com/index/gpt-5-6` still 403s; the aggregator-floated "Sol Ultra" math-proof claim has no OpenAI primary); Gemini 3.5 Pro GA still reported-only (~17 Jul, no model card/API/pricing); T1 quiet pre-14-Jul filing; all eight active threads rechecked, none crossed its Watch-for.

## Telegram — Run 20:00

OpenAI published a proof of the Cycle Double Cover Conjecture — a long-open graph-theory problem — and credits it entirely to its GPT-5.6 Sol Ultra model.

The conjecture asks whether every bridgeless graph can be covered by cycles that use each edge exactly twice. OpenAI says the model produced the proof; independent verification is pending.

"The proof in this note is entirely due to GPT 5.6 Sol Ultra"

a model's name on a maths proof is a new artifact — the audit is the real test now.

#OpenAI #GPT56 #AI #Math

https://cdn.openai.com/pdf/04d1d1e4-bc75-476a-97cf-49055cd98d31/cdc_proof.pdf

## Notes (archive only — not sent to Telegram)

### Run 07:00 — why skipped

- **Verified-feed spine checked (07:00):** Hugging Face newest models are community Qwen3-Coder GGUF quantizations and miniclay/test uploads — no frontier/major-lab open-weights drop; 12 Jul is Sunday, so arxiv cs.AI/CL/LG newest is still the 09 Jul weekday batch (no fresh weekend batch); OpenAI news newest is still the 10 Jul Deutsche Telekom customer story atop the already-shipped 09 Jul GPT-5.6 items — no new gate clearer; Anthropic newsroom newest still the 09 Jul governance/comms/product items (Bernanke→LTBT; "Inviting hard questions"; Claude-reflection; UST case study) — no model card, no Opus-line release; Google DeepMind blog shows no 10–12 Jul post and no Gemini 3.5 Pro GA.
- **Candidates weighed and dropped:**
  - **Gemini 3.5 Pro general availability** — the biggest thing the window could deliver, still **reported-only**: third-party reporting targets ~17 Jul; no model card, pricing, or benchmark primary; the public Gemini API still lists no 3.5 Pro GA endpoint. Not shipped → no gate (§5 modality: announced/rumored ≠ available).
  - **GPT-5.6 best-at-X (Coding Agent Index 80, "state of the art")** — still unshippable: `openai.com/index/gpt-5-6` **still 403s** to the fetcher; a best-at-X claim can't rest on Tier-2/aggregators alone (§5). No unlock this slot.
  - **Aggregator-floated items** (ByteDance Seedream 5.0 Pro image model; leaderboard/recap churn on llm-stats, pricepertoken, felloai) — Seedream released 08–09 Jul (not new since the last run) and only via aggregator/secondary, no ByteDance primary rendered; the rest fail §5 sourcing (no primary opened this session, uncertain freshness). Do not clear the gate.
- **Threads touched (all rechecked, none advanced):** T1 — no docket ruling; government response due 14 Jul; newsroom newest still the 09 Jul items → schedule holds. T5 — both GPT-5.6 beats shipped (system card 10 Jul 07:00; GA+pricing 10 Jul 20:00); `openai.com/index` primary still 403s → best-at-X still blocked. T8 — no Muse Spark 1.1 Evaluation Report benchmark extractable, no new tier/pricing on the Meta primary, Model API still public preview. T2 — no EUROPA-side movement. T3 — no concrete step out of the EU Action Plan; no new openable cyber-benchmark primary. T4 — no OpenAI/Broadcom silicon update. T6 — no Opus-line Claude 5 release, no new Claude 5 benchmark. T7 — no allowlisted independent Grok 4.5 benchmark, no EU availability, no model card, no new tier.
- **Verdict:** silence is correct (§2). A thorough Sunday-morning sweep against every major lab's own blog came back empty — the candidate releases (Gemini 3.5 Pro GA, GPT-5.6 best-at-X) are reported-only or blocked, not primary-confirmed. A padded post erodes the channel; skip.

### Run 13:00 — why skipped

- **Verified-feed spine checked (13:00):** Hugging Face newest models are still community Qwen/Gemma finetunes (Qwen2.5-Math exp, ttt-discover Qwen3, gemma4 Myanmar tokenizer, albedo-qwen3.6 merges) — no frontier/major-lab open-weights drop; 12 Jul is Sunday, so arxiv cs.AI/CL/LG newest is still the 09 Jul weekday batch; OpenAI news newest is still the 10 Jul Deutsche Telekom customer story atop the 09 Jul GPT-5.6 items — no new gate clearer; Anthropic newsroom newest still the 09 Jul governance/comms/product items — no model card, no Opus-line release; Google DeepMind blog shows no 10–12 Jul post and no Gemini 3.5 Pro GA.
- **Candidates weighed and dropped:**
  - **Gemini 3.5 Pro general availability** — still **reported-only**: third-party reporting still targets ~17 Jul; the public Gemini API still lists no `gemini-3.5-pro` GA endpoint; no model card, pricing, or benchmark primary. Not shipped → no gate (§5 modality: announced/rumored ≠ available).
  - **GPT-5.6 best-at-X (Coding Agent Index 80, "state of the art") + a floated "Sol Ultra" tier / Cycle-Double-Cover-Conjecture proof** — still unshippable: `openai.com/index/gpt-5-6` **still 403s** to the fetcher; the Sol Ultra math-proof story surfaces only via aggregators, no OpenAI primary rendered; a best-at-X claim can't rest on Tier-2/aggregators alone (§5). No unlock this slot.
- **Threads touched (all rechecked, none advanced):** T1 — no docket ruling; government response due 14 Jul; newsroom newest still the 09 Jul items → schedule holds. T5 — both GPT-5.6 beats shipped; `openai.com/index` primary still 403s → best-at-X still blocked; no Sol Ultra primary. T8 — no Muse Spark 1.1 Evaluation Report benchmark extractable, no new tier/pricing on the Meta primary, Model API still public preview. T2 — no EUROPA-side movement. T3 — no concrete step out of the EU Action Plan; no new openable cyber-benchmark primary. T4 — no OpenAI/Broadcom silicon update. T6 — no Opus-line Claude 5 release, no new Claude 5 benchmark. T7 — no allowlisted independent Grok 4.5 benchmark, no EU availability, no model card, no new tier.
- **Verdict:** silence is correct (§2). The Sunday early-afternoon sweep matched the 07:00 result — the two live candidates (Gemini 3.5 Pro GA, GPT-5.6 best-at-X / Sol Ultra) are reported-only or blocked, not primary-confirmed. Skip.

### Run 20:00 — POSTED (GPT-5.6 Sol Ultra / Cycle Double Cover Conjecture proof; T5 ADVANCE, T9 opened)

- **Chosen beat — why:** The floated "Sol Ultra math-proof" story that was aggregator-only at 07:00/13:00 now has an **OpenAI primary that opens and extracts** — two PDFs on `cdn.openai.com` (allowlisted, official OpenAI domain), published within the last ~8h (i.e. after the 13:00 run). This clears the gate as a net-new, primary-confirmed capability event: a frontier lab publishing a proof of a named open graph-theory conjecture and crediting it *entirely* to a specific model tier (GPT-5.6 Sol Ultra). Also confirms "Sol Ultra" as a real GPT-5.6 tier — T5's explicit Watch-for ("a new GPT-5.6 tier (e.g. Sol Ultra)"). Post URL = the proof PDF (contains the title, Theorem 1.1, and the AI-use attribution). https://cdn.openai.com/pdf/04d1d1e4-bc75-476a-97cf-49055cd98d31/cdc_proof.pdf
- **Modality guard (§5):** framed as *OpenAI published / credits* — NOT "proved" as a settled result. The note is a self-published preprint on OpenAI's CDN, not peer-reviewed; the conjecture has a documented history of proofs later found to have gaps (secondary — kept out of the post). Post carries "independent verification is pending" (mirrors the skill's sanctioned "Independent results pending" phrasing). No claim of Sol Ultra public availability (not stated by the primary).
- **Source-anchored claims:**
  - "OpenAI published a proof of the Cycle Double Cover Conjecture" <- proof PDF title "A PROOF OF THE CYCLE DOUBLE COVER CONJECTURE — OPENAI" + Theorem 1.1 "Every finite bridgeless undirected graph has a cycle double cover."
  - "a long-open graph-theory problem" <- abstract: "the cycle double cover conjecture, posed by Tutte, Itai and Rodeh, Szekeres, and Seymour" (multiple historical posers named).
  - "credits it entirely to its GPT-5.6 Sol Ultra model" <- "Statement of AI use. The proof in this note is entirely due to GPT 5.6 Sol Ultra and the writeup with Codex (with GPT 5.6 Sol)."
  - "every bridgeless graph can be covered by cycles that use each edge exactly twice" <- abstract: "asserts that every bridgeless undirected graph has a collection of cycles which covers every edge exactly twice."
  - "OpenAI says the model produced the proof" <- "The proof in this note is entirely due to GPT 5.6 Sol Ultra."
  - Pulled quote "The proof in this note is entirely due to GPT 5.6 Sol Ultra" <- verbatim from proof PDF, "Statement of AI use" section.
  - Reaction line ("a model's name on a maths proof is a new artifact — the audit is the real test now") is opinion — asserts no unsourced fact.
- **Not in the linked proof PDF (kept out of the post, per §5 "link contains every claim"):** "up to 64 concurrent agents" (that is in the *prompt* PDF `.../cdc_prompt.pdf`: "You have up to 64 concurrent agents available"); "under an hour" and "50 years / 1970s" dating (secondary reporting only — The Decoder, officechai, MLQ, Nextgov). Extracted the PDFs manually via stdlib zlib (no poppler/pypdf/cryptography in the env; WebFetch/Read PDF rendering both unavailable).
- **Threads touched:** **T5 ADVANCED** — "Sol Ultra" confirmed as a real GPT-5.6 tier via OpenAI primary; the tier-existence Watch-for is now satisfied (the best-at-X / Coding Agent Index claim is still separate and still blocked — `openai.com/index/gpt-5-6` still 403s). **T9 OPENED** — AI-for-mathematics / autonomous proof (this proof is its first beat; will keep developing as verification/peer-review lands). T1 — no docket ruling; government response due 14 Jul; newsroom newest still the 09 Jul items → schedule holds. T8 — HF newest still community uploads (incl. a community GLM-5.2 GGUF re-quant — GLM-5.2 is a 13/16 Jun Z.ai release, not new), no Meta primary movement. T2 — no EUROPA-side movement. T3 — no concrete step out of the EU Action Plan; no new openable cyber-benchmark primary. T4 — no OpenAI/Broadcom silicon update. T6 — no Opus-line Claude 5 release, no new Claude 5 benchmark. T7 — no allowlisted independent Grok 4.5 benchmark, no EU availability, no model card, no new tier.
- **Runners-up dropped:** Gemini 3.5 Pro GA — still reported-only (~17 Jul, now some reporting says 22–28 Jul); no model card / API / pricing primary; DeepMind blog shows no 3.5 Pro GA post. GPT-5.6 best-at-X (Coding Agent Index 80) — `openai.com/index/gpt-5-6` still 403s; Tier-2/aggregators can't sole-source a best-at-X claim (§5).
