# AI/LLM Briefing — 4 August 2026

## Telegram — Run 07:00

Alibaba's Qwen team released Qwen3.8-Max, a model that scales to 2.4 trillion parameters. The blog bills it as "a new bar for coding and cowork" — Qwen's term for agentic work an AI carries out on its own over long stretches.

Feels like a new Chinese frontier model every week now — I've stopped trying to rank them.

#Qwen #Alibaba #LLM #AI

https://qwen.ai/blog?id=qwen3.8

## Telegram — Run 13:00

SKIPPED — no material movement since 07:00. (T16/Qwen3.8-Max already posted at 07:00; watch-for unmet — no open weights on huggingface.co/Qwen yet, benchmark table still only in non-allowlisted secondary. No new model cards, no benchmark, no gov/policy primary.)

## Notes (archive only — not sent to Telegram)

### Run 07:00 — POST (T16 opened — Alibaba / Qwen frontier line)
- **Chosen beat:** Alibaba's Qwen team published a launch post for `Qwen3.8-Max` on `qwen.ai/blog?id=qwen3.8` ("A New Bar for Coding and Cowork"). A new frontier-class model from a top-6 lab — clears §2 ("New model / major version"). No existing thread (T10 = Moonshot/Kimi, T15 = DeepSeek are distinct actors), so opens **T16**. Surfaced via the llm-stats release tracker (Qwen 3.8 Max, ~2 days ago, 2.4T params) and confirmed against the qwen.ai primary. https://qwen.ai/blog?id=qwen3.8
- **Sourcing caveat (why this beat is minimal):** the `qwen.ai/blog?id=qwen3.8` primary is a JavaScript SPA that renders only the word "Qwen" to the fetcher — the body, availability line and benchmark table do not extract. No allowlisted Tier-2 (`techcrunch`/`reuters`/`apnews`/`theverge`/`arstechnica`) has covered it yet; the only full write-ups are Neowin and officechai, neither allowlisted (Neowin 403s anyway). Following the T7/Grok-4.5 precedent (SPA primary "resolves via domain-scoped search"), the post is anchored to the allowlisted primary and asserts **only** what a `qwen.ai`-scoped WebSearch confirmed: the model name/existence, the "2.4 trillion parameters" figure, and the verbatim "coding and cowork" framing. Everything the fetcher couldn't confirm from an allowlisted source was **dropped**.
- **Source-anchored claims:**
  - "Alibaba's Qwen team released Qwen3.8-Max" <- qwen.ai blog title "Qwen3.8-Max: A New Bar for Coding and Cowork" (Qwen = Alibaba's model team); llm-stats release tracker lists "Qwen 3.8 Max" as a new release ~2 days ago.
  - "a model that scales to 2.4 trillion parameters" <- qwen.ai-scoped WebSearch snippet, verbatim: "Qwen 3.8-Max scales to 2.4 trillion parameters and delivers comprehensive improvements across coding and work capabilities."
  - "the blog bills it as 'a new bar for coding and cowork'" <- verbatim qwen.ai blog title/subtitle: "A New Bar for Coding and Cowork."
  - "Qwen's term for agentic work an AI carries out on its own over long stretches" — jargon-unpacking gloss of "cowork," not a factual model-capability claim.
  - Reaction line ("a new Chinese frontier model every week… stopped trying to rank them") is a clearly-personal aside — asserts no unsourced fact, no prediction-as-fact.
- **Modality:** "released" — supported by the llm-stats release listing + the launch-post framing; deliberately no availability-channel claim ("via QwenCloud"), no open-weights-timing claim ("weights next week"), and no MoE/active-param count (95B active) — all of those live only in non-allowlisted secondary. Benchmark superiority (Neowin/officechai: beats GPT-5.6 Sol + Claude Fable 5 on 7 evals — CoWorkBench 74.8 vs 71.5, WorkSpaceBench 67.7 vs 65.6, JobBench 53.4 vs 45.4, WideSearch 81.9) and the "10 days continuous autonomous software development" line are **not shipped**: §5 bars Tier-2 as the sole source for a best-at-X claim, these outlets aren't even allowlisted, and the qwen.ai SPA would not render the table to the fetcher for verification.
- **Verified-feed spine checked:** **llm-stats release tracker** — surfaced Qwen 3.8 Max (2.4T, "coding and cowork") as the newest release (~Sun 2 Aug), atop DeepSeek-V4-Flash-0731 (31 Jul); this is the beat. **HF new-models** (sort=createdAt) — 4 Aug uploads are community finetunes/quants/GGUFs (DeepSeek-R1-0528-Qwen3-8B GGUF, Qwen3.5-2B community variants, Qwen2.5-1.5B finetunes); no official Qwen3.8-Max repo yet (consistent with weights pending). **OpenAI news RSS** — two 3 Aug items ("How we built a realtime system for responsive voice AI in six months"; "Circles powers telco personalization") are engineering/customer PR, no new model card or benchmark. **Anthropic newsroom** — newest still 30 Jul (Frontier Red Team). **arxiv cs.AI/CL/LG** — newest batch 3 Aug, no frontier-lab model/benchmark release (one paper notes a proof "first obtained using a fully automated Gemini-based agentic system … at Google" — academic mention, not a lab primary; T9 thread-note at most). **Google DeepMind blog** — newest still the Jul Gemini Robotics 2 cluster; no new GA language model.
- **Runner-up / not shipped:** the arxiv Gemini-agentic-proof mention (T9) — a single academic paper's methodology note, not an allowlisted lab announcement with a benchmark, no independent verdict → thread-note at most. OpenAI's 3 Aug GPT-Live realtime-voice engineering retrospective — no new model/benchmark primary. DeepSeek-V4-Pro (T15) still unshipped (mid-Aug reported window). No US-gov reply to the Pacing statement (T14), no imposed distillation enforcement (T13), no named CVE (T12), no first Article 50 enforcement action (T11).
- **Threads touched:** **T16 OPENED** (Alibaba / Qwen frontier line — Qwen3.8-Max released, 2.4T params, coding/cowork). All other active threads rechecked, none crossed its Watch-for: T15/T14/T13/T12/T11/T10/T9/T8/T7/T6/T5/T1/T2/T3/T4 quiet since last update or already advanced.

### Run 13:00 — why skipped
- Checked the verified-feed spine + targeted search; nothing cleared the §2 gate. **T16/Qwen3.8-Max** — watch-for unmet: no `huggingface.co/Qwen` repo yet (HF new-models 4 Aug are community finetunes/quants only, e.g. a DeepSeek-V4-Flash-0731 GGUF and a Qwen-3.6 variant), weights still reported "next week", benchmark table still only in non-allowlisted secondary (Neowin / the-decoder / MarkTechPost / warp2search). Can't ADVANCE the 07:00 post, and can't re-post the bare 2.4T launch. **OpenAI news RSS** — 3 Aug items are "How we built a realtime system for responsive voice AI…", "Circles powers telco personalization", and an "Apple is getting this wrong" opinion piece; no new model card / benchmark / pricing primary. **Anthropic newsroom** — newest still 30 Jul (Frontier Red Team). **arxiv cs.AI/CL/LG** — 3 Aug batch is academic (UEmbed embeddings, onepot-Bench chemistry, AURORA-LM); no frontier-lab release or standing-changing benchmark. **Google DeepMind** — no new GA model. No gov/policy primary today: no US-gov reply to the Pacing statement (T14), no imposed distillation enforcement (T13), no named CVE (T12), no first Article 50 enforcement action (T11), no DeepSeek-V4-Pro (T15).
